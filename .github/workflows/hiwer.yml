name: hiwer
on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      ssh:
        description: 'SSH connection to Actions'
        required: false
        default: 'false'
        
jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: 赋予权限
      uses: chmod 777 MinecraftClient-20230623-225-linux-x64

    - name: 启动
      run: ./MinecraftClient-20230623-225-linux-x64 hiwer.ini